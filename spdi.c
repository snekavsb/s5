#include <stdio.h>

int main(void) {
	char a[65];
	int i,s=0,f;
	scanf("%s",&a[i]);
	for(i=0;i!='\0';i++)
	{
		if(a[i]>0&&a[i]<9&&a[i]>'a'&&a[i]<'z'&&a[i]>'A'&&a[i]<'Z')
		{
			
		}
		else
	}
	
	return 0;
}
